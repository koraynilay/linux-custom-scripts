#!/bin/sh
git config --global credential.helper 'cache --timeout=240'
for a in $(ls | grep file | sed 's/ /*/g'); do
	cd "$a"
#	git init
#	git add .
#	git commit -m first
#	git push -u origin master
#	cp ~/linux-custom-scripts/push.sh .
	./push.sh
	echo $a
	cd /disks/I/Raccolte/various_files
done
